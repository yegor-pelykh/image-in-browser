/** @format */

/**
 * Enum representing different contrast modes.
 */
export enum ContrastMode {
  /**
   * Proportional contrast mode.
   */
  proportional,

  /**
   * S-curve contrast mode.
   */
  scurve,
}
