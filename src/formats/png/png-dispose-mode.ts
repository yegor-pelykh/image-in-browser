/** @format */

/**
 * Enum representing the dispose mode for PNG images.
 */
export enum PngDisposeMode {
  /**
   * No disposal specified.
   */
  none,

  /**
   * Dispose to background color.
   */
  background,

  /**
   * Dispose to previous image.
   */
  previous,
}
