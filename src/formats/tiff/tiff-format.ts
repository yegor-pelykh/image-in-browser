/**
 * Enum representing different TIFF formats.
 *
 * @format
 */

export enum TiffFormat {
  /** Invalid TIFF format */
  invalid,
  /** Unsigned integer TIFF format */
  uint,
  /** Signed integer TIFF format */
  int,
  /** Floating point TIFF format */
  float,
}
