/** @format */

/**
 * Enum representing the different WebP formats.
 */
export enum WebPFormat {
  /**
   * Undefined format.
   */
  undefined,
  /**
   * Lossy format.
   */
  lossy,
  /**
   * Lossless format.
   */
  lossless,
  /**
   * Animated format.
   */
  animated,
}
